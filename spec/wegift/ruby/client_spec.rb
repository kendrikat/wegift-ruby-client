require "spec_helper"

RSpec.describe Wegift::Ruby::Client do
  it "has a version number" do
    expect(Wegift::Ruby::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
